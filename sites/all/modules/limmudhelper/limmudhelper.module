<?php
function limmudhelper_views_query_alter(&$view, &$query) {
  if ($view->name == 'eloadas_idopontok_v') {
   // kpr($query);
    $query->fields['field_presentation_time_value']['field'] = "DATE_FORMAT(ADDTIME(field_data_field_presentation_time.field_presentation_time_value, SEC_TO_TIME(7200)), '%W')";
    //kpr($query);
  }
  
}

